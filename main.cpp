#include "bst.h"
#include "linkedlist.h"
#include <iostream>
using namespace std;

int main()
{
    /*
    BST tree;
    tree.get_root();
    tree.insert(5);
    tree.get_root();
    tree.insert(2);
    tree.insert(7);
    tree.insert(6);
    tree.insert(8);
    tree.insert(1);
    tree.insert(3);
    tree.get_root();
    tree.left_subtree->get_root();
    tree.left_subtree->left_subtree->get_root();
    tree.left_subtree->right_subtree->get_root();
    tree.right_subtree->get_root();
    tree.right_subtree->left_subtree->get_root();
    tree.right_subtree->right_subtree->get_root();
    tree.remove(7);
    tree.get_root();
    tree.left_subtree->get_root();
    tree.left_subtree->left_subtree->get_root();
    tree.left_subtree->right_subtree->get_root();
    tree.right_subtree->get_root();
    tree.right_subtree->left_subtree->get_root();
    int n = 1;
    do
    {
        cin >> n;
        cout << tree.find(n) << endl;
    }
    while (n != 0);
    */
   LinkedList list;
   list.length();
   list.insert(1);
   list.length();
   return 1;
}